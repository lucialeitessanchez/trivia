<form method="post" action="{{ path('app_pregunta_delete', {'idpregunta': preguntum.idpregunta}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ preguntum.idpregunta) }}">
    <button class="btn">Delete</button>
</form>
